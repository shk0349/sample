<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.mbc.jin10.login.LoginService">
	
	<select id="logininput" resultType="com.mbc.jin10.login.LoginDTO">
	select id,pw from member0910 where id=#{param1}and pw=#{param2}
	</select>


</mapper>